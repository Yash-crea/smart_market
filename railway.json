{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && cd grocerystore && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd grocerystore && python manage.py migrate --noinput && gunicorn grocerystore.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --workers 1",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
